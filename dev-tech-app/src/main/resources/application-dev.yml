server:
  port: 8090

spring:
  ai:
    ollama:
      base-url: http://180.76.52.221:11434

# Redis
redis:
  sdk:
    config:
      host: 180.76.52.221
      port: 16379
      pool-size: 10
      min-idle-size: 5
      idle-timeout: 30000
      connect-timeout: 5000
      retry-attempts: 3
      retry-interval: 1000
      ping-interval: 60000
      keep-alive: true
#      password: admin

logging:
  level:
    root: info
  config: classpath:logback-spring.xml
